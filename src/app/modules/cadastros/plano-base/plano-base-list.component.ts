import { Component, Inject } from '@angular/core';import { CommonModule } from '@angular/common';import { AgGridModule } from 'ag-grid-angular';import { ColDef, GridApi, GridOptions, GridReadyEvent, IDatasource, IGetRowsParams } from 'ag-grid-community';import { MatButtonModule } from '@angular/material/button';import { MatDialog, MatDialogModule, MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog';import { ReactiveFormsModule, FormBuilder, FormGroup, Validators } from '@angular/forms';import { BaseModalFormComponent } from '../../../shared/components/base-modal-form/base-modal-form.component';import { MatFormFieldModule } from '@angular/material/form-field';import { MatSelectModule } from '@angular/material/select';import { MatInputModule } from '@angular/material/input';import { MatDatepickerModule } from '@angular/material/datepicker';import { MatNativeDateModule } from '@angular/material/core';import { MatCheckboxModule } from '@angular/material/checkbox';import { OperadoraService } from '../operadora.service';import { HttpClient, HttpParams } from '@angular/common/http';import { environment } from '../../../../environments/environment';import { map } from 'rxjs/operators';import { AgGridLocaleService } from '../../../shared/services/ag-grid-locale.service';import { PlanoBaseService } from './plano-base.service';@Component({  selector: 'app-plano-base-list',  standalone: true,  imports: [CommonModule, AgGridModule, MatButtonModule, MatDialogModule],  template: `  <div class="d-flex justify-content-between align-items-center mb-2">    <h4>Planos Base</h4>    <button mat-stroked-button color="primary" (click)="openDialog()">Novo</button>  </div>  <ag-grid-angular class="ag-theme-quartz" style="width: 100%; height: 500px;"                   [gridOptions]="gridOptions" [datasource]="datasource" [columnDefs]="columnDefs" (gridReady)="onGridReady($event)">  </ag-grid-angular>  `})export class PlanoBaseListComponent {  datasource!: IDatasource;  private gridApi?: GridApi;  columnDefs: ColDef[] = [    { headerName: 'ID', field: 'id', width: 120, sortable: true, filter: true },    { headerName: 'Tipo de Plano', valueGetter: (p: any) => p.data?.tipoPlano?.descricao || '-', width: 200, sortable: true, filter: true },    { headerName: 'Operadora', valueGetter: (p: any) => p.data?.operadora?.razaoSocial || p.data?.operadora?.nomeFantasia || '-', width: 260, sortable: true, filter: true },    { headerName: 'Código ANS', field: 'codigoProdutoAns', width: 160, sortable: true, filter: true },    { headerName: 'Nome do Produto', field: 'nomeProduto', flex: 1, sortable: true, filter: true },    { headerName: 'Abrangência', valueGetter: (p: any) => p.data?.abrangencia?.descricao || '-', width: 180, sortable: true, filter: true },    { headerName: 'Tipo Contratação', valueGetter: (p: any) => p.data?.tipoContratacao?.descricao || '-', width: 200, sortable: true, filter: true },    { headerName: 'Ativo', field: 'ativo', width: 120, cellRenderer: (p: { value: boolean }) => (      p.value ? '<span class="badge text-bg-success">Ativo</span>' : '<span class="badge text-bg-danger">Inativo</span>'    ) },    { headerName: 'Ações', width: 180, cellRenderer: () => `      <button data-action="edit" class="btn btn-sm btn-outline-primary">Editar</button>      <button data-action="delete" class="btn btn-sm btn-outline-danger">Excluir</button>    `, onCellClicked: (p: any) => {      const action = (p.event?.target as HTMLElement)?.getAttribute('data-action');      if (action === 'edit') this.openDialog(p.data);      if (action === 'delete') this.remove(p.data);    }}  ];  private apiUrl = `${environment.apiUrl}/planos-base`;  gridOptions: GridOptions;  constructor(private http: HttpClient, private dialog: MatDialog, private fb: FormBuilder, private operadoraService: OperadoraService, private agGridLocaleService: AgGridLocaleService, private planoBaseService: PlanoBaseService) {    this.datasource = this.createDatasource();    this.gridOptions = {      ...this.agGridLocaleService.getDefaultGridOptions(),      rowModelType: 'infinite',      pagination: true,      paginationPageSize: 20,      cacheBlockSize: 20    };  }  onGridReady(params: GridReadyEvent): void { this.gridApi = params.api; }  createDatasource(): IDatasource {    return {      getRows: (params: IGetRowsParams) => {        const pageSize = this.gridOptions.paginationPageSize || 20;        const page = params.startRow / pageSize;        const sortModel = params.sortModel;        const sort = sortModel.length ? sortModel[0].colId : 'nomeProduto';        const order = sortModel.length ? (sortModel[0].sort || 'asc') : 'asc';        this.planoBaseService.getPlanosBasePaged(page, pageSize, sort, order).subscribe({          next: data => params.successCallback(data.content, data.totalElements),          error: () => params.failCallback()        });      }    };  }  openDialog(row?: any): void {    const form: FormGroup = this.fb.group({      id: [row?.id || null],      tipoPlanoId: [row?.tipoPlano?.id || null, Validators.required],      operadoraId: [row?.operadora?.idOperadora || null, Validators.required],      codigoProdutoAns: [row?.codigoProdutoAns || "", Validators.required],      nomeProduto: [row?.nomeProduto || "", Validators.required],      abrangenciaId: [row?.abrangencia?.id || null, Validators.required],      tipoContratacaoId: [row?.tipoContratacao?.id || null, Validators.required],      descricao: [row?.descricao || ""],      dataRegistroAns: [row?.dataRegistroAns ? new Date(row.dataRegistroAns) : null],      permiteComercializacao: [row?.permiteComercializacao ?? true],      ativo: [row?.ativo ?? true]    });    const operadoras$ = this.operadoraService.getOperadoras(0, 1000, 'razaoSocial', 'asc').pipe(map(p => p.content));    const tiposPlano$ = this.http.get<any[]>(`${this.apiUrl.replace('planos-base','tipos-plano')}`);    const abrangencias$ = this.http.get<any>(`${environment.apiUrl}/abrangencias-geograficas`, { params: new HttpParams().set('page','0').set('size','1000') }).pipe(map((p: any) => p.content));    const tiposContratacao$ = this.http.get<any>(`${environment.apiUrl}/tipos-contratacao`, { params: new HttpParams().set('page','0').set('size','1000') }).pipe(map((p: any) => p.content));    const ref = this.dialog.open(PlanoBaseDialogComponent, { width: '900px', data: { form, title: row ? 'Editar' : 'Novo', operadoras$, tiposPlano$, abrangencias$, tiposContratacao$ } });    ref.afterClosed().subscribe(res => {      if (res?.saved) {        const payload: any = {          id: form.value.id,          tipoPlano: form.value.tipoPlanoId ? { id: form.value.tipoPlanoId } : null,          operadora: form.value.operadoraId ? { idOperadora: form.value.operadoraId } : null,          abrangencia: form.value.abrangenciaId ? { id: form.value.abrangenciaId } : null,          tipoContratacao: form.value.tipoContratacaoId ? { id: form.value.tipoContratacaoId } : null,          codigoProdutoAns: form.value.codigoProdutoAns,          nomeProduto: form.value.nomeProduto,          descricao: form.value.descricao,          dataRegistroAns: form.value.dataRegistroAns ? (new Date(form.value.dataRegistroAns)).toISOString().substring(0,10) : null,          permiteComercializacao: form.value.permiteComercializacao,          ativo: form.value.ativo        };        const obs = payload.id ? this.http.put(`${this.apiUrl}/${payload.id}`, payload) : this.http.post(this.apiUrl, payload);        obs.subscribe(() => this.gridApi?.refreshInfiniteCache());      }    });  }  remove(row: any): void { this.http.delete(`${this.apiUrl}/${row.id}`).subscribe(() => this.gridApi?.refreshInfiniteCache()); }}@Component({  selector: 'app-plano-base-dialog',  standalone: true,  imports: [CommonModule, BaseModalFormComponent, ReactiveFormsModule, MatFormFieldModule, MatSelectModule, MatCheckboxModule, MatInputModule, MatDatepickerModule, MatNativeDateModule],  styles: [    `.mat-mdc-form-field { width: 100%; }`,    `.d-flex { display: flex; }`,    `.justify-content-end { justify-content: flex-end; }`,    `.mt-3 { margin-top: 1rem; }`,    `.mb-3 { margin-bottom: 1rem; }`,    `.me-2 { margin-right: .5rem; }`  ],  template: `    <app-base-modal-form [title]="data.title" (save)="onSave()" (cancel)="onCancel()">    <form [formGroup]="data.form">      <div class="row">        <div class="col-md-6">          <mat-form-field>            <mat-label>Tipo de Plano</mat-label>            <mat-select formControlName="tipoPlanoId" required>              <mat-option *ngFor="let tp of (data.tiposPlano$ | async)" [value]="tp.id">{{ tp.descricao }}</mat-option>            </mat-select>          </mat-form-field>        </div>        <div class="col-md-6">          <mat-form-field>            <mat-label>Operadora</mat-label>            <mat-select formControlName="operadoraId" required>              <mat-option *ngFor="let op of (data.operadoras$ | async)" [value]="op.idOperadora">{{ op.razaoSocial || op.nomeFantasia }}</mat-option>            </mat-select>          </mat-form-field>        </div>      </div>      <div class="row">        <div class="col-md-4">          <mat-form-field>            <mat-label>Código ANS</mat-label>            <input matInput formControlName="codigoProdutoAns" required>          </mat-form-field>        </div>        <div class="col-md-8">          <mat-form-field>            <mat-label>Nome do Produto</mat-label>            <input matInput formControlName="nomeProduto" required>          </mat-form-field>        </div>      </div>      <div class="row">        <div class="col-md-6">          <mat-form-field>            <mat-label>Abrangência</mat-label>            <mat-select formControlName="abrangenciaId" required>              <mat-option *ngFor="let ab of (data.abrangencias$ | async)" [value]="ab.id">{{ ab.descricao }}</mat-option>            </mat-select>          </mat-form-field>        </div>        <div class="col-md-6">          <mat-form-field>            <mat-label>Tipo de Contratação</mat-label>            <mat-select formControlName="tipoContratacaoId" required>              <mat-option *ngFor="let tc of (data.tiposContratacao$ | async)" [value]="tc.id">{{ tc.descricao }}</mat-option>            </mat-select>          </mat-form-field>        </div>      </div>      <div class="row">        <div class="col-md-6">          <mat-form-field>            <mat-label>Data Registro ANS</mat-label>            <input matInput [matDatepicker]="picker" formControlName="dataRegistroAns">            <mat-datepicker-toggle matSuffix [for]="picker"></mat-datepicker-toggle>            <mat-datepicker #picker></mat-datepicker>          </mat-form-field>        </div>        <div class="col-md-6 d-flex align-items-center gap-3 mt-3">          <mat-checkbox formControlName="permiteComercializacao">Permite Comercialização</mat-checkbox>          <mat-checkbox formControlName="ativo">Ativo</mat-checkbox>        </div>      </div>      <mat-form-field>        <mat-label>Descrição</mat-label>        <textarea matInput formControlName="descricao" rows="3"></textarea>      </mat-form-field>    </form>  </app-base-modal-form>  `})export class PlanoBaseDialogComponent {  constructor(@Inject(MAT_DIALOG_DATA) public data: any, private ref: MatDialogRef<PlanoBaseDialogComponent>) {}  onSave(): void { this.ref.close({ saved: true }); }  onCancel(): void { this.ref.close(); }}
